server {
    listen 80;
    server_name 192.168.1.103;

    # Optional: Redirect HTTP to HTTPS (recommended for production)
    # return 301 https://$host$request_uri;

    location / {
        # Forward requests to your Node.js application running on localhost:3000
        proxy_pass http://localhost:8000;
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection 'upgrade';
        proxy_set_header Host $host;
        proxy_cache_bypass $http_upgrade;
    }
}

# Example of an HTTPS server block (requires SSL certificates)
# server {
#     listen 443 ssl;
#     server_name your_domain.com www.your_domain.com;
#
#     ssl_certificate /etc/letsencrypt/live/your_://domain.com; # Managed by Certbot
#     ssl_certificate_key /etc/letsencrypt/live/your_://domain.com; # Managed by Certbot
#     include /etc/letsencrypt/options-ssl-nginx.conf;
#     ssl_dhparam /etc/letsencrypt/ssl-dhparams.pem;
#
#     location / {
#         proxy_pass http://localhost:3000;
#         # ... other proxy settings as above
#     }
# }
